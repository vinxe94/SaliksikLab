from django.urls import path
from .views import (
    ResearchOutputListCreateView,
    ResearchOutputDetailView,
    ApproveOutputView,
    DownloadFileView,
    PreviewFileView,
    ReviseOutputView,
    VersionHistoryView,
    RollbackVersionView,
    BackupView,
    RestoreView,
    StatsView,
    ExportCSVView,
    ArchiveDocumentListCreateView,
    ArchiveDocumentDetailView,
    ArchiveDocumentDownloadView,
    ArchiveDocumentPreviewView,
    ArchiveDocumentVersionHistoryView,
    ArchiveDocumentReviseView,
    ArchiveDocumentReviewView,
    DepartmentListCreateView,
    DepartmentDetailView,
    CourseListCreateView,
    CourseDetailView,
)

urlpatterns = [
    path('departments/', DepartmentListCreateView.as_view(), name='department-list-create'),
    path('departments/<int:pk>/', DepartmentDetailView.as_view(), name='department-detail'),
    path('courses/', CourseListCreateView.as_view(), name='course-list-create'),
    path('courses/<int:pk>/', CourseDetailView.as_view(), name='course-detail'),
    path('archives/', ArchiveDocumentListCreateView.as_view(), name='archive-list-create'),
    path('archives/<int:pk>/', ArchiveDocumentDetailView.as_view(), name='archive-detail'),
    path('archives/<int:pk>/versions/', ArchiveDocumentVersionHistoryView.as_view(), name='archive-versions'),
    path('archives/<int:pk>/revise/', ArchiveDocumentReviseView.as_view(), name='archive-revise'),
    path('archives/<int:pk>/review/', ArchiveDocumentReviewView.as_view(), name='archive-review'),
    path('archives/<int:pk>/download/', ArchiveDocumentDownloadView.as_view(), name='archive-download'),
    path('archives/<int:pk>/download/<int:version_id>/', ArchiveDocumentDownloadView.as_view(), name='archive-version-download'),
    path('archives/<int:pk>/preview/', ArchiveDocumentPreviewView.as_view(), name='archive-preview'),
    path('archives/<int:pk>/preview/<int:version_id>/', ArchiveDocumentPreviewView.as_view(), name='archive-version-preview'),
    path('', ResearchOutputListCreateView.as_view(), name='output-list-create'),
    path('stats/', StatsView.as_view(), name='output-stats'),
    path('export/csv/', ExportCSVView.as_view(), name='output-export-csv'),
    path('backup/', BackupView.as_view(), name='backup'),
    path('restore/', RestoreView.as_view(), name='restore'),
    path('<int:pk>/', ResearchOutputDetailView.as_view(), name='output-detail'),
    path('<int:pk>/approve/', ApproveOutputView.as_view(), name='output-approve'),
    path('<int:pk>/download/', DownloadFileView.as_view(), name='output-download'),
    path('<int:pk>/download/<int:file_id>/', DownloadFileView.as_view(), name='file-download'),
    path('<int:pk>/preview/', PreviewFileView.as_view(), name='output-preview'),
    path('<int:pk>/preview/<int:file_id>/', PreviewFileView.as_view(), name='file-preview'),
    path('<int:pk>/revise/', ReviseOutputView.as_view(), name='output-revise'),
    path('<int:pk>/versions/', VersionHistoryView.as_view(), name='output-versions'),
    path('<int:pk>/rollback/', RollbackVersionView.as_view(), name='output-rollback'),
]
